# 個人HPの編集ガイド

このサイトは、GitHub Pagesの標準ビルド（Jekyll）で公開しています。ソースのフォルダーと公開URLを分けて管理します。

## まず開くファイル

| 変更したい場所 | 編集するファイル |
| --- | --- |
| Home | `index.html` |
| 自己紹介・研究・経歴 | `pages/profile/profile.html` |
| ブログ一覧 | `pages/blog/blog.html` |
| ブログ記事の本文 | 対応する `pages/blog/blog-*.html` |
| 資料のリンク集 | `pages/materials/materials.html` |
| 学会の日程 | `pages/materials/conference-schedule.html` |
| 一次資料・研究資料庫 | `pages/materials/resource-archive.html` |
| 写真ギャラリー | 対応する `pages/gallery/gallery-*.html` |
| イラスト一覧 | `pages/gallery/illustrations.html` |
| デザインの一覧 | `pages/design/design.html` |

## 見た目・動作を変える場所

- `css/classical.css`：現在の主要ページで使う共通デザイン。先頭の `:root` に色・文字・余白の変数があります。
- `css/classical-sub.css`：記事・資料庫・写真詳細などの補助デザイン。
- `js/classical.js`：共通のメニュー、画像表示、言語切替などの動作。
- `assets/conference-schedule.css`：学会日程と資料トップのデザイン。末尾付近にアンソロピックの配置・リンク表示があります。
- `css/blog-index.css` / `js/blog-index.js`：ブログ一覧の見た目と絞り込み。
- `assets/`：画像、ロゴ、記事別のCSSなど。

どのCSS・JavaScriptが使われるかは、各HTMLの `stylesheet` と `<script src="…">` を検索して確認できます。`css/styles.css` や `js/script.js` などもあるため、ファイル名だけで判断して削除しないでください。

## 資料リンクを追加する例

`pages/materials/materials.html` の `anthropic-resources` 内では、1つの `<article>` が1件のリンクです。同じ位置に次のブロックを追加します。

```html
<article>
  <h3>
    <a href="https://example.com/" target="_blank" rel="noopener noreferrer">
      サイト名
    </a>
  </h3>
  <p>何が読めるサイトかを日本語で説明します。</p>
</article>
```

リンク文字の青緑色と下線はCSSで設定しています。リンクごとに色を書く必要はありません。

## HTMLを読む順番

1. `<head>`：検索結果の説明、SNS共有画像、CSSの読み込み。
2. `<header class="site-header">`：共通のサイト名・ナビゲーション。
3. `<main>`：ページ本文。普段の文章やリンクの変更はここです。
4. `<footer>`：ページ下部の案内。
5. `<script>`：ページの動作を読み込む部分。

文章はタグの間、リンク先は `href`、画像ファイルは `src`、画像の説明は `alt` を編集します。`class` は見た目や動作、`id` はページ内リンクの目印に使われます。

## 確認して公開する

ローカルで表示する場合は、このフォルダーで `python3 tools/preview.py` を実行して、ブラウザーで `http://localhost:8000/` を開きます。終了は Ctrl+C です。

1. 編集したページをPC幅・スマートフォン幅で確認します。
2. `git diff --check` で余分な空白などを確認します。
3. `git diff -- ファイル名` で変更内容を確認します。
4. `git add -- ファイル名` で今回の変更だけを選び、コミットして `git push origin main` します。

CSSの変更がブラウザーに反映されないときは、HTMLのCSS読み込みURL末尾の `?v=...` を更新します。

## 作業記録と整形

作業記録は `_private/作業履歴.md` にあります。`_private/` はGitの対象外で、確認用データや公開しない資料を置く場所です。

2026-09-21にHTML・CSS・JavaScriptをPrettier 3.6.2で整形しました。HTMLは幅100文字を目安にし、埋め込みスクリプトの整形を無効にしています。構成やファイル名は維持しています。

その時点で別の変更があった `pages/blog/blog.html`、`pages/blog/blog-ai-robotics-roadmap.html`、`assets/roadmap-article.css` は整形対象から外しています。

## フォルダー構成（2026-09-21）

```text
index.html          Home
404.html            エラーページ
pages/
  blog/             ブログ一覧と記事
  gallery/          写真・イラスト
  materials/        資料・学会日程
  profile/          自己紹介・個人の活動
  design/           デザイン・アニメーション
css/                共通・ページ別のスタイル
js/                 共通・ページ別の動作
assets/             画像・ロゴ・既存の専用素材
tools/             ローカル確認用スクリプト
_private/           公開しない作業資料
```

移動したファイルの先頭には、公開URLを指定する設定があります。

```yaml
---
layout: null
permalink: /materials.html
---
```

この例では、編集するファイルは `pages/materials/materials.html`、公開URLは以前と同じ `/materials.html` です。CSSとJavaScriptも同じ仕組みです。HTML中の参照はソースの保存場所ではなく公開URLなので、移動後も元の参照を維持しています。

通常の文章編集では先頭の設定を変更しません。新しいファイルには重複しない `permalink` を付けてください。ファイルを直接ブラウザーで開くのではなく、上記のプレビュースクリプトを使います。GitHub側でビルドされるため、公開設定の変更や追加のインストールは不要です。
